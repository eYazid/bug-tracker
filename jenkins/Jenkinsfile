pipeline {
    agent any

    stages {
        stage('Unit Tests - Backend') {
            steps {
                sh 'echo "Running unit tests for backend..."'
                dir('bugtracker-backend') {
                    go test -v ./...
                }
            }
        }
    }
}
